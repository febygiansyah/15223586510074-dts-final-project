import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#009688",
    },
    secondary: {
      main: "#f57f17",
    },
  },
});

export default theme;
